name: tthc
description: A Very Good Project created by Very Good CLI.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # LOCALIZATION
  flutter_localizations:
    sdk: flutter
  intl: ^0.17.0

  # STATE MANAGEMENT
  flutter_riverpod: ^1.0.2
  flutter_hooks: ^0.18.1
  hooks_riverpod: ^1.0.2
  dvhcvn:
    git:
      url: "https://github.com/definev/dvhcvn.git"
      path: transformers/dart-dvhcvn
      ref: master

  # Styles
  flex_color_scheme: ^4.1.1
  animations: ^2.0.2
  simple_animations: ^4.0.1

dev_dependencies:
  bloc_test: ^9.0.1
  flutter_test:
    sdk: flutter
  mocktail: ^0.2.0
  very_good_analysis: ^2.4.0

flutter:
  uses-material-design: true
  generate: true
